[project]
name = "ftm-fixture"
version = "0.1.0"
description = "FTM Overview QA fixture — see qa.config.yml for the specific scenario."
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "ddtrace>=2.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
